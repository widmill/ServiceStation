package com.example.servicestation.repository;

import com.example.servicestation.entity.RepairedAuto;
import org.springframework.data.repository.CrudRepository;

public interface RepairedAutoRepo extends CrudRepository<RepairedAuto, Long> {
}
